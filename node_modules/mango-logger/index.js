const Logger = require('./lib/logger')
const RequestLogger = require('./lib/request/request.logger')

module.exports = {
    Logger,
    LOGGER: new Logger({
        saveFormat: 'date'
    }),
    RequestLogger,
    REQUESTLOGGER: new RequestLogger({
        saveFormat: 'date'
    }),
}